<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="009-1" author="loghealer">
        <comment>Replace full unique constraint with partial unique index on active projects only</comment>
        <dropUniqueConstraint tableName="project" constraintName="uk_project_key"/>
        <sql>CREATE UNIQUE INDEX uk_project_key_active ON project (project_key) WHERE is_active = true</sql>
    </changeSet>

</databaseChangeLog>
